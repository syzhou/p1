#include "Ordered_container.h"

#include <stdio.h>
#include <stdlib.h>

int compare(int *a, int* b);
int main() {
	struct Ordered_container* numbers = OC_create_container();
}
int compare(int *a, int* b) {
	return (*a - *b);
}
