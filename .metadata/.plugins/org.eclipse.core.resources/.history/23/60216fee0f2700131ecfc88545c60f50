#include "Ordered_container.h"

#include <stdio.h>
#include <stdlib.h>

int compare(int *a, int* b);
int main() {
	struct Ordered_container* numbers = OC_create_container((OC_comp_fp_t)compare);
	int* a = 0;
	printf("%d", a);
}
int compare(int *a, int* b) {
	return (*a - *b);
}
